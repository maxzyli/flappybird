import { ResourceManager, SpriteAtlas } from "@oasis-engine/core";
import { AssetConfig } from "../types";
import { SchemaResource } from "./SchemaResource";
export declare class SpriteAtlasResource extends SchemaResource {
    static defaultAtlas: SpriteAtlas;
    load(resourceManager: ResourceManager, assetConfig: AssetConfig): Promise<SpriteAtlasResource>;
    setMeta(): void;
    getProps(): {};
    update(): void;
}
