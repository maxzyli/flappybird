import { Entity, MeshRenderer } from "@oasis-engine/core";
export declare class Model extends MeshRenderer {
    private _props;
    constructor(entity: Entity);
    get material(): any;
    set material(mtl: any);
    setProps(props?: any): void;
    updateProp(key: string, value: string | number): void;
}
