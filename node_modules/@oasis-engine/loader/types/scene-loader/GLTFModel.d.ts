import { Animator, AnimatorController, Component } from "@oasis-engine/core";
import { GLTFResource } from "../gltf/GLTFResource";
/**
 * @deprecated
 * Temporarily only for editor use.
 * Remove when editor finish change from gltf to prefab.
 */
export declare class GLTFModel extends Component {
    private _animatorController;
    private _speed;
    private _animator;
    private _asset;
    private _glTFEntity;
    private _clipPreview;
    private _hasBuiltNode;
    private _controllerUpdateFlag;
    get asset(): GLTFResource;
    set asset(value: GLTFResource);
    get animatorController(): AnimatorController;
    set animatorController(animatorController: AnimatorController);
    get speed(): number;
    set speed(speed: number);
    get animator(): Animator;
    get clipPreview(): string;
    set clipPreview(value: string);
    constructor(entity: any);
    /**
     * Init.
     * @param props - Init props
     */
    init(props: any): void;
    update(): void;
    /**
     * @override
     */
    _onEnable(): void;
    /**
     * @override
     */
    _onDisable(): void;
    _playState(): void;
    _playDefaultState(): void;
}
