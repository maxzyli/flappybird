import { AssetConfig, ResourceManager, SchemaResource } from 'oasis-engine';
import '@oasis-engine/spine';
export declare class SpineResource extends SchemaResource {
    load(resourceManager: ResourceManager, assetConfig: AssetConfig): Promise<any>;
}
