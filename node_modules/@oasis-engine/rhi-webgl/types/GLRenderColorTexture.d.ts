import { IPlatformRenderColorTexture, RenderColorTexture, TextureCubeFace } from "@oasis-engine/core";
import { GLTexture } from "./GLTexture";
import { WebGLRenderer } from "./WebGLRenderer";
/**
 * The texture in WebGL platform is used for the output of color information in off-screen rendering.
 */
export declare class GLRenderColorTexture extends GLTexture implements IPlatformRenderColorTexture {
    /**
     * Create render color texture in WebGL platform.
     */
    constructor(rhi: WebGLRenderer, texture: RenderColorTexture);
    /**
     * Get the pixel color buffer according to the specified cube face and area.
     * @param face - You can choose which cube face to read if it's cube texture
     * @param x - X coordinate of area start
     * @param y - Y coordinate of area start
     * @param width - Area width
     * @param height - Area height
     * @param out - Color buffer
     */
    getPixelBuffer(face: TextureCubeFace | null, x: number, y: number, width: number, height: number, out: ArrayBufferView): void;
}
