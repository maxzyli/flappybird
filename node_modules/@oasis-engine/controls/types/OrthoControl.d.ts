import { Camera, Entity, Script } from "oasis-engine";
/**
 * The camera's 2D controller, can zoom and pan.
 */
export declare class OrthoControl extends Script {
    cameraEntity: Entity;
    camera: Camera;
    private _zoomSpeed;
    private _zoomScale;
    private _zoomScaleUnit;
    private _zoomMinSize;
    private _zoomMaxSize;
    private _isPanStart;
    private _panStartPos;
    private _panStart;
    private _panEnd;
    private _panDelta;
    /**
     * The zoom speed.
     */
    get zoomSpeed(): number;
    set zoomSpeed(value: number);
    constructor(entity: Entity);
    onUpdate(dt: number): void;
    /**
     * Zoom in.
     */
    zoomIn(): void;
    /**
     * Zoom out.
     */
    zoomOut(): void;
    /**
     * Start pan.
     * @param x - The x-axis coordinate (unit: pixel)
     * @param y - The y-axis coordinate (unit: pixel)
     */
    panStart(x: number, y: number): void;
    /**
     * Panning.
     * @param x - The x-axis coordinate (unit: pixel)
     * @param y - The y-axis coordinate (unit: pixel)
     *
     * @remarks Make sure to call panStart before calling panMove.
     */
    panMove(x: number, y: number): void;
    /**
     * End pan.
     */
    panEnd(): void;
    private _getZoomScale;
    private _handlePan;
}
