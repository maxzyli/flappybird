import { TypeLayer } from '../../LottieResource';
import TransformFrames from '../TransformFrames';
import { Entity } from 'oasis-engine';
/**
 * @internal
 */
export default class BaseLottieElement {
    transform: TransformFrames;
    is3D: boolean;
    offsetTime: number;
    name: string;
    index: number;
    stretch: number;
    parent: any;
    inPoint: any;
    outPoint: any;
    timeRemapping: any;
    width: number;
    height: number;
    visible: boolean;
    entity: Entity;
    startTime: number;
    treeIndex: number[];
    private childLayers;
    constructor(layer: TypeLayer);
    reset(): void;
    update(frameNum?: number, isParentVisible?: boolean): void;
    /**
     * add child layer
     */
    addChild(node: any): void;
    destroy(): void;
}
