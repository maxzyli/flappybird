import { Entity, Sprite, SpriteAtlas, SpriteRenderer } from 'oasis-engine';
import BaseLottieLayer from './BaseLottieLayer';
/**
 * @internal
 */
export default class SpriteLottieLayer extends BaseLottieLayer {
    sprite: Sprite;
    entity: Entity;
    spriteRenderer: SpriteRenderer;
    constructor(layer: any, atlas: SpriteAtlas, entity: Entity, index: number);
}
