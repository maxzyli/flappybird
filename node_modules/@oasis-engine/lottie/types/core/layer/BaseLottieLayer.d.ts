import { TypeLayer } from '../../LottieResource';
import TransformFrames from '../TransformFrames';
/**
 * @internal
 */
export default class BaseLottieLayer {
    transform: TransformFrames;
    is3D: boolean;
    offsetTime: number;
    name: string;
    index: number;
    stretch: number;
    startTime: number;
    parent: any;
    inPoint: any;
    outPoint: any;
    timeRemapping: any;
    width: number;
    height: number;
    visible: boolean;
    private isOverlapLayer;
    private isOverlapMode;
    constructor(layer: TypeLayer);
    update(frameNum?: number, forceUpdate?: boolean): void;
}
