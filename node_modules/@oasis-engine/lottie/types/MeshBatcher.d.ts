import { Buffer, Entity, MeshRenderer } from 'oasis-engine';
/**
 * @internal
 */
export declare class MeshBatcher extends MeshRenderer {
    vertices: Float32Array;
    indices: Uint16Array;
    vertexBuffer: Buffer;
    private indexBuffer;
    private verticesLength;
    private indicesLength;
    constructor(entity: Entity);
    init(l: number): void;
    setBufferData(): void;
}
