import { Loader, LoadItem, ResourceManager, Entity } from "oasis-engine";
/**
 * @internal
 */
export declare class LottieLoader extends Loader<Entity> {
    load(item: LoadItem, resourceManager: ResourceManager): Promise<Entity>;
}
