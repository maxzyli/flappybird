import { Entity } from "../Entity";
import { Texture2D } from "../texture/Texture2D";
import { MeshRenderer } from "./MeshRenderer";
import { Skin } from "./Skin";
/**
 * SkinnedMeshRenderer.
 */
export declare class SkinnedMeshRenderer extends MeshRenderer {
    private static _blendShapeMacro;
    private static _blendShapeNormalMacro;
    private static _blendShapeTangentMacro;
    private static _jointCountProperty;
    private static _jointSamplerProperty;
    private static _jointMatrixProperty;
    private static _blendShapeWeightsProperty;
    private static _maxJoints;
    matrixPalette: Float32Array;
    jointNodes: Entity[];
    jointTexture: Texture2D;
    private _hasInitJoints;
    private _mat;
    /** Whether to use joint texture. Automatically used when the device can't support the maximum number of bones. */
    private _useJointTexture;
    private _skin;
    private _blendShapeWeights;
    /**
     * The weights of the BlendShapes.
     * @remarks Array index is BlendShape index.
     */
    get blendShapeWeights(): Float32Array;
    set blendShapeWeights(value: Float32Array);
    /**
     * Constructor of SkinnedMeshRenderer
     * @param entity - Entity
     */
    constructor(entity: Entity);
    /**
     * Skin Object.
     */
    get skin(): Skin;
    set skin(skin: Skin);
    _initJoints(): void;
    private findByNodeName;
    /**
     * Generate joint texture.
     * Format: (4 * RGBA) * jointCont
     */
    createJointTexture(): void;
}
