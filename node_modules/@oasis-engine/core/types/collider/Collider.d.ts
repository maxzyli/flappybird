import { Component } from "../Component";
import { Entity } from "../Entity";
import { Ray, Vector3 } from "@oasis-engine/math";
import { HitResult } from "../HitResult";
/**
 * Define collider data.
 */
export declare class Collider extends Component {
    private static _ray;
    /**
     * @param {Entity} entity
     */
    constructor(entity: Entity);
    _onEnable(): void;
    _onDisable(): void;
    protected _updateHitResult(ray: Ray, distance: number, outHit: HitResult, origin: Vector3, isWorldRay?: boolean): void;
    protected _getLocalRay(ray: Ray): Ray;
}
