/**
 * Used to update tags.
 */
export declare class UpdateFlag {
    private _flags;
    /** Flag. */
    flag: boolean;
    constructor(_flags?: UpdateFlag[]);
    /**
     * Destroy.
     */
    destroy(): void;
}
