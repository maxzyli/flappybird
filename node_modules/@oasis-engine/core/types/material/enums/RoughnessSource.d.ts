/**
 * Roughness source.
 */
declare enum RoughnessSource {
    /** Use metallic texture's alpha as smooth. */
    MetallicTextureAlpha = 0,
    /** Use base texture's alpha as smooth. */
    BaseTextureAlpha = 1
}
