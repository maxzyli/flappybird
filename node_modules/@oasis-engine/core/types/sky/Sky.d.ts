import { Mesh } from "../graphic";
import { Material } from "../material";
/**
 * Sky.
 */
export declare class Sky {
    /** Material of the sky. */
    material: Material;
    /** Mesh of the sky. */
    mesh: Mesh;
}
